interface:
  display_name: "Publish HTML"
  short_description: "Publish HTML through a configured ATML service"
  default_prompt: "Use $publish-html to publish this HTML project and return its protected URL and PIN."
